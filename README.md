How apply loop "for" to loop "do while"?

 let Simple = [],
 questions = [
     "Simple question",
     "Simple question",
     "Simple question",
     "Simple question",
     "Simple question"
 ];

// for (let i=0; i < questions.length; i++){
//     Simple[i] = prompt(questions[i], "" );
// }
//     document.write(Simple);
